<?php

/**
 * |--------------------------------------------------------------------------
 * | Storefronts Frontend Settings
 * |--------------------------------------------------------------------------
 * 
 * Dedicated to settings specifically for the front-end only.
 * 
 * If a setting applies to another section as well, then it should go in the common 
 * storefronts.php config.
 */
return [
		/**
		 * |--------------------------------------------------------------------------
		 * | Production Pagination count
		 * |--------------------------------------------------------------------------
		 * 
		 * Determines how many products to paginate by on a store page.
		 */
		 'product-pagination-count' => 10
];
